const FORMAT_INVALID = { message: 'Invalid email or password' };
const BAD_REQUEST = { message: 'All fields must be filled' };
const TOKEN_NOT_VALID = { message: 'Token not found' };

export {
  FORMAT_INVALID,
  BAD_REQUEST,
  TOKEN_NOT_VALID,
};
